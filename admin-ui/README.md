# PulseFlow admin UI

Vite + React + TypeScript + Tailwind.

Local development (expects the gateway on `http://localhost:8080`):

```bash
npm install
npm run dev
```

Production build is produced by `npm run build` and served via nginx in Docker.
