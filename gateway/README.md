# PulseFlow API gateway

Kotlin + Spring Cloud Gateway. Routes `/api/v1/**` to the workflow service.

Run locally:

```bash
./gradlew bootRun
```

Defaults are in `src/main/resources/application.yml`.
